package garden.model;

public class WaterSource extends Source {

    public WaterSource(int waterStrength, int waterLength) {
        this.setStrength(waterStrength);
        this.setLength(waterLength);
    }
}
